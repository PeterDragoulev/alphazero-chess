# Chess Engine

Building a chess engine in stages: classical search first, then learned
evaluation, working toward an AlphaZero-style policy-value network with MCTS.

- `1_search/` — minimax and alpha-beta over a material evaluator
